---
alwaysApply: true
Before running any Node.js commands, ensure you are using the correct Node.js version:

1. Run `nvm use v24.4.0` to switch to the required Node.js version
2. Verify the version with `node --version` to confirm it shows v24.4.0

If you haven't installed this Node.js version yet:
1. Run `nvm install v24.4.0` first
2. Then run `nvm use v24.4.0`

This project requires Node.js v24.4.0 to ensure compatibility and consistent behavior across all environments.
---
