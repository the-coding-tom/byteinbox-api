import { Injectable } from '@nestjs/common';

@Injectable()
export class AudiencesValidator {
  // No specific validation needed for GET endpoints
  // Validation can be added here if needed for future POST/PUT endpoints
}
